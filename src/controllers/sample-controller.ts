

const DUMMY_USER = [{
  id: 'u1',
  name: 'TTK user',
  email: 'user@example.com',
  password: 'p1'
}]

const getSampleData = (req: Request, res: Response, next: Function) => {
  // res.json({ DUMMY_USER })
}