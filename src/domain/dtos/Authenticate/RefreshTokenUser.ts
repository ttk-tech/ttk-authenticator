/**
 * Data Transfer Object (DTO) representing a refresh token user.
 *
 * @interface
 */
export interface IRefreshTokenUserDTO {
  /**
   * The unique identifier for the refresh token.
   */
  refreshTokenID: string
}